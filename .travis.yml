branches:
 only:
  - develop
  - master

git:
 depth: 1

language: csharp
solution: src/AcspNet.sln

before_script:
 - chmod +x .ci/nunit.sh

script:
 - xbuild /p:Platform="Any CPU" /p:Configuration=Release src/AcspNet.sln
 - .ci/nunit.sh .ci/tests.nunit
